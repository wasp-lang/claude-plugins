---
description: Start the Wasp development server as a background task
---

Explicitly invoke the `start-dev-server` skill to start the Wasp development servers as a background task.

Example:
```
Run the `start-dev-server` skill as a background task in the current Claude Code session.
```
